services:
  - type: web
    name: traderai
    env: node
    region: singapore
    buildCommand: npm install && npm run build
    startCommand: npx vite preview --host 0.0.0.0 --port 10000
    envVars:
      - key: NODE_VERSION
        value: 20
